fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
# Üretim veritabanı sürücüsü (PostgreSQL)
psycopg[binary]>=3.2
pydantic[email]>=2.8
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.2
# Faaliyet takvimi PDF'lerinden metin çıkarımı
pypdf>=5.0
# Isteğe bağlı: SPA / bot korumalı siteler için tarayıcı ile render
playwright>=1.45
# FCM push
# [requests] onemli: google.auth.transport.requests modulu requests
# kutuphanesini istiyor. Onsuz ice aktarma patliyor ve bildirimler
# sessizce atlaniyordu.
google-auth[requests]>=2.30
